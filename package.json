{
  "name": "strava-data-analysis",
  "version": "1.0.0",
  "description": "Perform data analysis on my Strava activity data.",
  "main": "index.js",
  "scripts": {
    "emulate": "firebase emulators:start",
    "emulate:no-func": "firebase emulators:start --only firestore,storage",
    "shell": "npx firebase functions:shell",
    "cors-update": "gcloud storage buckets update $npm_config_bucket_name --cors-file=cors.json && gcloud storage buckets describe $npm_config_bucket_name --format='json(cors_config)'",
    "deploy:functions": "firebase deploy --only functions",
    "deploy:hosting": "firebase deploy --only hosting"
  },
  "keywords": [
    "Strava",
    "data",
    "analysis",
    "API"
  ],
  "author": "Krish A. Patel",
  "license": "ISC",
  "dependencies": {
    "firebase": "^11.10.0"
  },
  "devDependencies": {
    "firebase-tools": "^15.1.0"
  }
}